name: 🕵️ Verify Vault Sync (Audit)

on:
  push:
    branches: [ "main", "feature/*" ] # J'ai ajouté feature/* pour vos tests
  workflow_dispatch:

jobs:
  audit-configuration:
    name: 🛡️ Audit Variables & Secrets
    runs-on: ubuntu-latest
    
    # 👇 CORRECTION DES PERMISSIONS ICI
    permissions:
      contents: read   # Pour checkout
      variables: read  # Pour 'gh variable list'
      # Note: Il n'existe pas de permission pour lister les secrets avec GITHUB_TOKEN

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v4

      # ------------------------------------------------------------------------
      # ÉTAPE 1 : Lister les VARIABLES (OK avec permission)
      # ------------------------------------------------------------------------
      - name: 📋 List Repository Variables
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "::group::📂 All Available Variables"
          gh variable list || echo "No variables found or permission denied."
          echo "::endgroup::"

      # ------------------------------------------------------------------------
      # ÉTAPE 2 : Audit des SECRETS (Adapté pour la sécurité)
      # ------------------------------------------------------------------------
      - name: 🔐 Audit Secret Configuration
        run: |
          echo "::group::🗝️ Secret Inventory Strategy"
          echo "ℹ️ SECURITY NOTE: The GITHUB_TOKEN is architecturally forbidden"
          echo "   from listing secret names via CLI to prevent reconnaissance."
          echo ""
          echo "👉 We will validate Vault Sync by checking secret presence directly"
          echo "   in the 'Deep Dive' step below."
          echo "::endgroup::"

      # ------------------------------------------------------------------------
      # ÉTAPE 3 : Preuve de Fonctionnement (Vérification Specifique)
      # ------------------------------------------------------------------------
      - name: ✅ Verify Vault Injection
        env:
          # On mappe les secrets Vault attendus
          DB_PASS: ${{ secrets.VAULT_DEMO_DB_PASSWORD }}
          API_KEY: ${{ secrets.VAULT_DEMO_API_KEY }}
        run: |
          echo "::group::🔍 Deep Dive Verification"
          
          echo "Testing specific secrets injected by Terraform/Vault..."
          echo "---------------------------------------------------"

          # Test 1 : DB PASSWORD
          if [ -n "$DB_PASS" ]; then
            # On affiche juste la longueur pour prouver que ce n'est pas vide
            echo "✅ VAULT_DEMO_DB_PASSWORD is Present (Length: ${#DB_PASS} chars)"
          else
            echo "❌ VAULT_DEMO_DB_PASSWORD is MISSING or EMPTY!"
            echo "   -> Terraform sync failed or Secret Engine is empty."
            exit 1
          fi

          # Test 2 : API KEY
          if [ -n "$API_KEY" ]; then
            echo "✅ VAULT_DEMO_API_KEY is Present (Length: ${#API_KEY} chars)"
          else
            echo "❌ VAULT_DEMO_API_KEY is MISSING or EMPTY!"
            exit 1
          fi
          
          echo "---------------------------------------------------"
          echo "🎉 SUCCESS: Vault secrets are correctly synchronized!"
          echo "::endgroup::"